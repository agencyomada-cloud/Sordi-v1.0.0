base_folder/
dist/
dist-isolation/
node_modules/
src-tauri/target/
src-tauri/gen/
.DS_Store
.env
.env.*.local
*.log

# Local runtime data / real business records — never belongs in version control
*.sqlite
*.sqlite3
*.db
*.xlsx
*.xls

# node_modules/, dist/, .env, and *.log above already match at any depth
# (no leading slash), so they already cover apps/*, packages/* too — only
# the local Postgres data directory needs its own explicit entry.
apps/api/.pgdata/
